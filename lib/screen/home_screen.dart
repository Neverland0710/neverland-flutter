// lib 폴더에 폴더 하나 더 만들어 파일 생성

import 'package:flutter/material.dart';

//stless로 자동완성
class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
